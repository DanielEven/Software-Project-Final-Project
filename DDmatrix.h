#ifndef DD_MATRIX_H
#define DD_MATRIX_H

#include "matrix.h"

/* A function which creates the Diagonal Degree Matrix from Weighted Adjacency Matrix. */
Matrix *Create_DD_matrix(Matrix *WAmatrix);

#endif
